<h1><%= @task.title %></h1>
<br>
<%= @task.details %>
<br>
<% if @task.completed == true %>
  <%= check_box_tag @task.title, 1, @task.completed %> <%= "This task is completed" %>
<% else %>
  <%= check_box_tag @task.title, 0 %> <%= "This task is not completed yet" %>
<% end %>

<div>
  <%= link_to "Back to list 👈", tasks_path %>
</div>

<div>
  <%= link_to "Edit 👈", edit_path(@task) %>
</div>
